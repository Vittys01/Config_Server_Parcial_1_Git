server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Vittys01/Config_Server_Parcial_1_Git
          default-label: main
          force-pull: true
          search-paths: configurationGit